spring:
    application:
        name: cbss-identity-service
    config:
        import: configserver:http://localhost:8888
    datasource:
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://${database.host}:${database.port}/${database.name}
        username: admin
        password: admin
    jpa:
        hibernate:
            ddl-auto: create
    sql:
        init:
            mode: always
